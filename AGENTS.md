# AGENTS.md

See [CLAUDE.md](./CLAUDE.md) for architecture, invariants, and conventions, and
[README.md](./README.md) for how to run and test the app.

The HTTP API documents itself at `/docs` while the app is running — that is the only endpoint
reference, because a hand-written one drifts.
