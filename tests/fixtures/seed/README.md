Test content, not product content.

Deliberately small and purpose-built: enough to exercise search ranking, backlinks to a
source, wikilink resolution and an export round trip, and nothing more. The application used
to ship starter notes in `seed/`, and the suite quietly depended on them — so a change to that
content could break tests, and the tests could not say what content they actually needed.
